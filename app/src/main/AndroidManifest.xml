<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.pos_ui">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.POS_UI">
        <activity android:name=".UpdatePassword"></activity>
        <activity android:name=".CashierBalanceReport" />
        <activity android:name=".CreateCashierView" />
        <activity android:name=".UpdateItemView" />
        <activity android:name=".CashierHomePage" />
        <activity android:name=".DailySalesReportActivity" />
        <activity android:name=".StocksView" />
        <activity android:name=".ManagerHomeView" />
        <activity android:name=".LoginView" />
        <activity

            android:name=".MainActivity">
<!--            android:screenOrientation="landscape"-->
<!--            tools:ignore="LockedOrientationActivity">-->


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>